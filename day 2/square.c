#include <stdio.h>

int main()
{
    int radius;
    scanf("%d",&radius);
    int area = radius*radius;
    printf("Write a Program to find the area of a aquare.\n");
    printf("Use Formula: ");
    printf("%d %d",radius,area);
    

    return 0;
}